valor = int(input('valor?'))
sobra = 0
conta = 0
def calculo(valor):
    while x != 1 | 0:
        conta = valor // 2
        sobra = conta % 2
        valores = []
        valores.append(conta)
        x = sobra
print(conta)